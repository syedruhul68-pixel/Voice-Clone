# for Gradle
args=-Xmx4g -Dkotlin.daemon.jvm.options=-Xmx4g

# AndroidX / Jetifier (must be lowercase)
android.useAndroidX=true
android.enableJetifier=true

# Compose compiler in-process (optional but stabilizes some CI/mobile builds)
kotlin.compiler.execution.strategy=in-process
